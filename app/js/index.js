window.onload = function(e){ 
// Get the necessary elements first
const skills = document.getElementsByClassName('one')[0];
const value = document.getElementById("two").scrollTop

/* second.classList.remove('text-white')
second.classList.add('text-orange-500') */

console.log(value)
}